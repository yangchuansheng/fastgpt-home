# Commit normalized technical-content import authority

External technical-content delivery packages are immutable provenance inputs to explicit ingestion runs. For each run, a committed normalized manifest and decision ledger are the reproducible repository authority for accepted technical-page identities, import operations, provenance, corrections, denials, and approved exceptions; publish projections must be derived from and agree with them.
