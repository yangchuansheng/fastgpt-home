# Use locale and canonical path as technical-page identity

A technical page is uniquely identified within technical content by `(locale, owner-relative canonical path)`. Canonical URLs, source filenames, and final slugs are projections of that identity, so identical final slugs remain valid when the complete identities differ.
